<h1>Posts#new</h1>
<p>Find me in app/views/posts/new.html.erb</p>
<!--
like link_to, form_for is a Rails helper method that generates HTML code. This
code allows a user to submit a post title and body, thus creating a new post.
-->

<h1>New Post</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for posts</p>
    <ul>
      <li>Make sure it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @post do |f| %>
      <div class="form-group">
<!-- We use f.label to display title. This functionality is provided by form_for.
# form_for yields a form builder object which in turns provides label. f.label in
#turn creates an HTML <label> tag for the object that is specified.
  ex. <label for= "title">Title</label>
-->
        <%= f.label :title %>
<!-- text_field is another method that Formhelper provideas and creates an input tag.-->
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post body" %>
      </div>
      <div class="form-group">
<!-- f.submit yields an input tag of type submit.-->
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
      <% end %>
    </div>
  </div>
